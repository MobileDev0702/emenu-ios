# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EMenu' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EMenu
  pod 'SideMenu'

  target 'EMenuTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EMenuUITests' do
    # Pods for testing
  end

end
